One pin in this project is deliberately unplaced. It belongs to the *Thread* "Practice: place the loose pin".\
Click on the 'eye' toggle to view the pins that are assigned to this scene. You should see this:

![image-8.png](assets/image-8.png)

***

💬

*Try this: open the Pincushion (the floating pin icon in the editor) and find the unplaced pin. Drag it into a paragraph here to anchor it. That is the whole workflow: decide where a *Thread* touches the draft, then drop the pin there.*

Place the pin on this line.

After placing it, type `/threads` and select the practice *Thread* to see its pin list update.
