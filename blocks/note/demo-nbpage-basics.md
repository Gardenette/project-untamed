Notebooks live on the Bookshelf. Type `/notebooks` to open it.

Each notebook holds pages, and pages can hold sub-pages. Use notebooks for research, reference material, and anything that supports the draft without belonging to it.

To add a page from anywhere, type `/newpage`.
